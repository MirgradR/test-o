name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Init:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]
    steps:
     # 1. Клонирование кода
      - uses: actions/checkout@v5

      # 2. Установка Node.js
      - name: Start the Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node-version }}

      # 3. Установка зависимостей
      - name: Install dependencies
        run: npm install

      # 4. Сборка проекта
      - name: Build the project
        run: npm run build

      # 5. Запускаем сборку проеёкта
      - name: Build the project
        run: npm run build
        
      # 6 Запуск тестов и линтинга
      - name: Run tests
        run: npm run test
      - name: Run linting
        run: npm run lint
      - name: Run tests coverage
        run: npm run test:coverage
      - name: Run tests UI
        run: npm run test:ui
